{
  "name": "ecommerce-artisans-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

